# =============================================================================
# Le Maia Admin — Vercel (projeto separado do catálogo)
# Copie para admin/.env.local (dev) ou painel Vercel (produção)
# =============================================================================

# URL base da API Railway — SEM /api no final (rotas já incluem /api/admin/...)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Produção:
# NEXT_PUBLIC_API_URL=https://sua-api.up.railway.app
