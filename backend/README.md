# Backend legado (Express + Railway)

Este diretório foi **substituído** pela API em Route Handlers do Next.js:

- Código ativo: `frontend/app/api/`
- Lógica: `frontend/lib/server/`
- Prisma: `frontend/prisma/`

Não é mais necessário deploy no Railway. Mantido apenas como referência histórica até remoção completa.
